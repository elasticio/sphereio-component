var BaseService, PaymentService,
  extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

BaseService = require('./base');

PaymentService = (function(superClass) {
  extend(PaymentService, superClass);

  function PaymentService() {
    return PaymentService.__super__.constructor.apply(this, arguments);
  }

  PaymentService.baseResourceEndpoint = '/payments';

  return PaymentService;

})(BaseService);

module.exports = PaymentService;
