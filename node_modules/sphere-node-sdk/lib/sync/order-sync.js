var BaseSync, OrderSync, OrderUtils, _,
  __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

_ = require('underscore');

BaseSync = require('./base-sync');

OrderUtils = require('./utils/order');


/**
 * Order Sync class
 */

OrderSync = (function(_super) {
  __extends(OrderSync, _super);

  function OrderSync() {
    this._utils = new OrderUtils;
  }


  /**
   * @override
   */

  OrderSync.prototype._doMapActions = function(diff, new_obj, old_obj) {
    var allActions;
    allActions = [];
    allActions.push(this._mapActionOrNot('status', (function(_this) {
      return function() {
        return _this._utils.actionsMapStatusValues(diff, old_obj);
      };
    })(this)));
    allActions.push(this._mapActionOrNot('returnInfo', (function(_this) {
      return function() {
        return _this._utils.actionsMapReturnInfo(diff, old_obj);
      };
    })(this)));
    allActions.push(this._mapActionOrNot('deliveries', (function(_this) {
      return function() {
        return _this._utils.actionsMapDeliveries(diff, old_obj);
      };
    })(this)));
    return _.flatten(allActions);
  };

  return OrderSync;

})(BaseSync);

module.exports = OrderSync;
